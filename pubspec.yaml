name: note
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"
  flutter: ">=3.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  path_provider: ^2.0.13
  hive_flutter: ^1.1.0
  uuid: ^3.0.7
  intl: ^0.19.0
  fluttertoast: ^8.2.1
  sizer: ^2.0.15

dev_dependencies:
  hive_generator:
  build_runner:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/animation/